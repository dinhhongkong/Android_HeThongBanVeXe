<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home"
    app:startDestination="@id/bookTicketFragment">
    <fragment
        android:id="@+id/bookTicketFragment"
        android:name="com.android.home.ticket.BookTicketFragment"
        android:label="fragment_book_ticket"
        tools:layout="@layout/fragment_book_ticket" >
        <action
            android:id="@+id/action_bookTicketFragment_to_profileFragment"
            app:destination="@id/profileFragment" />
    </fragment>
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.android.home.profile.ProfileFragment"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" >
        <action
            android:id="@+id/action_profileFragment_to_bookTicketFragment"
            app:destination="@id/bookTicketFragment" />
    </fragment>
</navigation>